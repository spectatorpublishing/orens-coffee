import React from 'react';
import styled from 'styled-components';
import { device } from '../device';

const HomeContainer = styled.div`

`;

const Home = () => (
    <HomeContainer> This is home
        
    </HomeContainer>
);

export default Home;